app_name = 'invoice'

urlpatterns = [
    # path('<int:booking_pk>/pay/', pay_for_booking, name='pay-for-booking'),
    # path('<int:booking_pk>/pay/success/', PaymentSuccessView.as_view(), name='payment-success'),
    # path('<int:booking_pk>/pay/failure/', PaymentFailureView.as_view(), name='payment-failure'),
    # path('<int:booking_pk>/invoice/', download_invoice, name='download-invoice'),
]
